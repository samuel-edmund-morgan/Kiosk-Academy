<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_image">

    <ImageView
        android:id="@+id/ssu_emblem"
        android:layout_width="110dp"
        android:layout_height="110dp"
        android:layout_marginTop="80dp"
        android:layout_marginEnd="150dp"
        android:contentDescription="@string/ssu_emblem"
        android:src="@drawable/ssu_emblem"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/academy_emblem"
        android:layout_width="110dp"
        android:layout_height="110dp"
        android:layout_marginStart="32dp"
        android:contentDescription="@string/ssu_emblem"
        android:src="@drawable/academy_emblem"
        app:layout_constraintBottom_toBottomOf="@+id/ssu_emblem"
        app:layout_constraintStart_toEndOf="@+id/ssu_emblem"
        app:layout_constraintTop_toTopOf="@+id/ssu_emblem" />

    <TextView
        android:id="@+id/main_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/main_message"
        android:fontFamily="@font/montserrat_black"
        android:text="@string/main_message"
        android:textAlignment="center"
        android:textAllCaps="true"
        android:textColor="@color/white"
        android:textSize="170sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/bullet_hole"
        android:layout_width="62dp"
        android:layout_height="57dp"
        android:layout_marginTop="500dp"
        android:alpha="0.7"
        android:contentDescription="@string/bullet_hole"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.565"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/bullet_hole" />

</androidx.constraintlayout.widget.ConstraintLayout>